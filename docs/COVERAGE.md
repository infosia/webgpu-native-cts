# COVERAGE — Port Status

Tracks how much of the upstream CTS has been ported, per area and per file. Denominators are the
upstream `.spec.ts` file counts at the pinned revision (see [UPSTREAM.md](UPSTREAM.md)).

**Pinned upstream:** gpuweb/cts `b507bd1` (2026-05-28) — 683 `.spec.ts` files under `src/webgpu/`.

> This is a living document. Update it as files are ported (or deliberately skipped). The file
> table starts empty; only the area summary's denominators are pre-filled.

## Status legend

| Status | Meaning |
|--------|---------|
| **ported** | All (or all in-scope) tests in the file are ported and run on at least one backend |
| **partial** | Some tests ported; others skipped/unimplemented (list which) |
| **N/A** | No native analog (e.g. WebIDL `TypeError` coercion tests, web-platform-only); not portable |
| **deferred** | In scope eventually but intentionally postponed (e.g. shader execution) |
| **todo** | Not started (default) |

A test marked `.unimplemented()` in a ported file counts the file as **partial**.

## Area summary

| Area | Upstream files | Ported | Partial | N/A | Deferred | Todo |
|------|---------------:|-------:|--------:|----:|---------:|-----:|
| `api/validation` | 129 | 42 | 8 | 0 | 0 | 79 |
| `api/operation` | 72 | 14 | 30 | 4 | 0 | 24 |
| `shader/validation` | 207 | 0 | 0 | 0 | 207 | 0 |
| `shader/execution` | 239 | 0 | 0 | 0 | 239 | 0 |
| `compat` | 15 | 0 | 0 | 0 | 0 | 15 |
| `web_platform` | 13 | 0 | 0 | 13 | 0 | 0 |
| `idl` | 3 | 0 | 0 | 3 | 0 | 0 |
| other (root/examples/etc.) | 5 | 0 | 0 | 0 | 0 | 5 |
| **Total** | **683** | **56** | **38** | **20** | **446** | **123** |

**Workflow bulk ports.** `api/validation` is being ported in parallel batches (a Workflow fans out one
Sonnet agent per file = a full faithful port, then a single compile-verify stage; Claude reviews + Dawn-
oracle-verifies + all-backend-sweeps + commits). **Batch 1** (commit `7088b2a`) ported 10 files —
`query_set/{create,destroy}`, `encoding/cmds/debug`, `texture/{float32_filterable,destroy,bgra8unorm_storage}`,
`debugMarker`, `queue/destroyed/query_set`, `non_filterable_texture`, `dispatch` — all Dawn-green (0 fail).
`dispatch` (both tests) and the `bgra8unorm_storage` canvas-context cases are `.unimplemented()` (no native
C analog). It surfaced **F-057** (yawgpu cross-HAL: `texture_cube_array<f32>` errors the shader module — 8
of `non_filterable_texture`'s 160 cases). **Batch 2** (commit `8f01f97`) ported 15 more —
`render_pipeline/{primitive_state,float32_blendable,multisample_state,misc,depth_stencil_state}`,
`encoding/{beginComputePass,beginRenderPass,encoder_state}`,
`encoding/cmds/{render/setPipeline,render/setIndexBuffer,render/setVertexBuffer,index_access}`,
`texture/rg11b10ufloat_renderable`, `getBindGroupLayout`, `shader_module/entry_point` — all Dawn-green (full
matrices: `depth_stencil_state` 1600, `entry_point` 1242, `misc` 744 cases). Review fixes: a
`timestamp-query` feature gate on the `begin*Pass` mismatched-device case; `encoder_state`'s
`pass_end_invalid_order` left `.unimplemented()` (native eager vs JS lazy error model). It surfaced three more
yawgpu findings (all cross-HAL): **F-058** (over-requires `depthCompare`+`depthWriteEnabled`;
`depth_stencil_state`, 10), **F-059** (storage-texture-format support gap; `render_pipeline/misc`
`storage_texture,format`, ~366) — **both now fixed** (yawgpu `8b42e5d`; `depth_stencil_state` `pass=1600`,
`storage_texture,format` `pass=720` on both HALs) — and **F-060** (WGSL errors on `texture_external`;
`render_pipeline/misc` `external_texture`, 2 cases; **OPEN**). wgpu-native shows expected validation gaps + aborts on several of these groups (the panic family,
contained via `--isolate`; to be added to the Windows-generated `expectations/wgpu-native.{txt,crash.txt}`
on the next regen). **Batch 3** (commit `4b7c350` + fixes `376d9c3`) ported 15 more —
`render_pipeline/{shader_module,inter_stage,resource_compatibility,overrides}`, `shader_module/overrides`,
`encoding/{createRenderBundleEncoder,render_bundle,cmds/{compute_pass,copyBufferToBuffer,setBindGroup},queries/resolveQuerySet}`,
`queue/{submit,writeBuffer,destroyed/buffer}` (+ `encoding/cmds/render_pass`, an upstream 0-test stub) —
all Dawn-green (full matrices: `resource_compatibility` 15754, `createRenderBundleEncoder` 550). Three
review fixes (render-bundle DS-readonly mismatch; resolveQuerySet's encoder-command errors **defer to
finish()** not eager; queue/submit invalid-CB via double-finish). It surfaced three more cross-HAL yawgpu
findings: **F-061** (over-rejects compatible pipeline-layout binding kinds, 80), **F-062** (over-rejects
compatible render-bundle attachment signatures, 30), **F-063** (inter-stage interpolation-sampling
mis-validated, 12) — **all three now fixed and re-verified on both HALs** (2026-06-09:
`resource_compatibility` `pass=123`, `render_bundle` `pass=21`, `inter_stage` `pass=26`, fail=0 on Metal and
Vulkan/MoltenVK).

Notes on the pre-classified rows:

- **`web_platform` (13) → N/A**: canvas, `importExternalTexture` from `HTMLVideoElement`/`VideoFrame`,
  workers — these depend on the web platform and have no native C-API analog. See
  [00-overview](00-overview.md) non-goals.
- **`idl` (3) → N/A**: WebIDL surface/constant tests; the C API has no IDL layer.
- **`shader/validation` (207) + `shader/execution` (239) → deferred**: shader validation is
  mid-term; shader execution is the largest deferred effort. See [07-roadmap](07-roadmap.md)
  phases 5–6. (Some `shader/validation` files may be reclassified to `todo` once that phase
  starts.)
- **`api/regression`, `shader/regression`**: 0 files at this revision.

These classifications are provisional; revisit per file when the area is worked.

## Phase 1 (vertical slice) — done

The slice ported one real file to prove the harness end-to-end on wgpu-native (see
[07-roadmap §Phase 1](07-roadmap.md)):

| Upstream file | Status | Tests ported | Notes |
|---------------|--------|--------------|-------|
| `api/validation/buffer/create.spec.ts` | partial | `limit`, `new_usages` | `size`/`usage`/`createBuffer_invalid_and_oom` unimplemented; uses `GpuTest` instead of `AllFeaturesMaxLimitsGPUTest` (no extra features needed for these two) |

## Full per-file table

Append rows as files are touched. Keep paths relative to upstream `src/webgpu/`. Only list files
that are no longer `todo` (the area summary covers the rest).

| File | Status | Tests ported / total | Skipped / N/A tests | Notes |
|------|--------|----------------------|---------------------|-------|
| `api/validation/buffer/create.spec.ts` | partial | 4 / 5 (`limit`, `new_usages`, `usage`, `createBuffer_invalid_and_oom`) | `size` (shouldThrow RangeError — no C analog) | green on **yawgpu** (all 4: `usage`=78 cases/156 subcases, `c_i_a_o`=8, `limit`=3, `new_usages`=10). On wgpu-native all pass **except `usage`, which aborts wgpu-native** (see [FINDINGS F-001](FINDINGS.md)). Fixture deviation `GpuTest` |
| `api/validation/buffer/destroy.spec.ts` | partial | 3 / 4 (`all_usages`, `twice`, `error_buffer`) | `while_mapped` (needs mapAsync map/unmap) | green on wgpu-native, **yawgpu & Dawn** (`all_usages`=10, `twice`=6, `error_buffer`=1; error buffers crash no backend); fixture deviation `GpuTest` |
| `api/validation/createSampler.spec.ts` | partial | 1 / 2 (`lodMinAndMaxClamp`) | `maxAnisotropy` (WebIDL number coercion `-1`/`undefined` — no C `uint16` analog) | green on wgpu-native **and yawgpu** (`lodMinAndMaxClamp`=1 case/49 subcases — both validate lod clamp ranges). First float-`Value` test; fixture deviation `GpuTest` |
| `api/validation/encoding/cmds/clearBuffer.spec.ts` | ported | 8 / 8 — COMPLETE (`buffer_usage`, `default_args`, `size_alignment`, `offset_alignment`, `overflow`, `out_of_bounds`, `buffer_state`, `buffer,device_mismatch`) | — | green on **yawgpu & Dawn**. **T20** implements `buffer,device_mismatch` (T18 `mismatchedDevice()` + `createBufferOnMismatchedDevice`); green on all three. wgpu-native still aborts on `size_alignment`/`out_of_bounds` ([F-002](FINDINGS.md)) and the `buffer_state` destroyed case ([F-004](FINDINGS.md)); contained via `--isolate`. Command-encoder + `undefined`-`Value` + resource-state; fixture deviation `GpuTest` |
| `api/validation/encoding/cmds/copyBufferToBuffer.spec.ts` | ported | 8 / 8 — COMPLETE (`buffer_usage`, `copy_size_alignment`, `copy_offset_alignment`, `copy_overflow`, `copy_out_of_bounds`, `copy_within_same_buffer`, `buffer_state`, `buffer,device_mismatch`) | — | green on **yawgpu & Dawn**. **T20** implements `buffer,device_mismatch` (green on all three). wgpu-native still aborts on the 3 `buffer_state` destroyed-submit cases ([F-004](FINDINGS.md)); contained via `--isolate`. Fixture deviation `GpuTest` |
| `api/validation/createBindGroupLayout.spec.ts` | ported | 11 / 11 — COMPLETE (all tests; no `device_mismatch` test in this file) | — | **T13** builds the **BGL binding-entry taxonomy** in `capability_info.h` (`kShaderStageCombinations`, buffer/sampler/texture-sample/storage-access type lists, 11 string-key `allBindingEntries`, per-entry `validStages`) + `getCompatibilityModeLimits()` (chains `WGPUCompatibilityModeLimits` for the per-stage storage limits — present in all 3 pinned headers, **no re-baseline needed**). The 3 `visibility` tests: **Dawn passes all (reference)**; `…buffer_type` green on all three; `visibility` + `…storage_texture_access` surface **F-016** (yawgpu rejects read-write storage on the core `r32*` formats — 8 cases) and **F-017** (wgpu-native aborts on storage-texture BGL entries — `:*` prefixes). Entry params use a **string-key** encoding (query-identity deviation, documented in the file header). **T14** adds the storage access-mode predicates (`isTextureFormatUsableAs{ReadOnly,ReadWrite}StorageTexture` + `kReadWriteStorageTextureFormats`/tier2 table) and ports `multisampled_validation` (7), `storage_texture,layout_dimension` (7), `storage_texture,formats` (303) — reusing `kStorageTextureAccessValues`/`kTextureSampleTypes` (T13) + `kTextureViewDimensions` (T9). **Dawn passes all (reference)**; surfaces **F-018** (yawgpu over-restricts BGL storage textures: 1D view dimension + `rgba8snorm` write-only/read-only — 3 cases) and **F-019** (wgpu-native aborts on an undefined BGL view dimension); `storage_texture,formats` also re-crashes wgpu-native (F-017, `:*` prefix). **T15** adds `max_dynamic_buffers` (3 cases — buffer-type dynamic-offset limit enforcement, COMPUTE-only; buffer-type→limit accessors over base `WGPULimits`) — **green on all three** (no findings; all backends enforce the dynamic-buffer limit). **T16** completes the file with `max_resources_per_stage,{in_bind_group_layout,in_pipeline_layout}` (11+11 cases) — adds the per-stage-limit-class layer (`bglEntryPerStageLimitClass`, `getBindingLimitForBindingType` over base+compat limits, `pickExtraBindingTypesForPerStage`, `kShaderStages`, `kMaxResourcesCases`). **Dawn + yawgpu pass all 11 of each (yawgpu correctly enforces per-stage limits — no finding)**; wgpu-native crashes the 8 sampler/sampled-texture/storage-texture `maxedEntry` cases (`conv.rs:1669` eager-panic, [F-019](FINDINGS.md), 16 exact lines). `b0`/`b1` deviation for the array param; fixture `GpuTest` |
| `api/validation/createPipelineLayout.spec.ts` | ported | 7 / 7 — COMPLETE (`number_of_bind_group_layouts_exceeds_the_maximum_value`, `number_of_dynamic_buffers_exceeds_the_maximum_value`, `bind_group_layouts,null_bind_group_layouts`, `immediate_data_size`, `bind_group_layouts,device_mismatch`, `bind_group_layouts,create_pipeline_with_null_bind_group_layouts`, `bind_group_layouts,set_pipeline_with_null_bind_group_layouts`) | — | **T17** ports `number_of_dynamic_buffers_exceeds_the_maximum_value` (1 case/12 subcases — per-pipeline dynamic-buffer limit) reusing the T15/T16 limit helpers (`bufferTypeMaxDynamicBuffersLimit`, `getBindingLimitForBindingType`, now returning 0 for `visibility=None`); no findings. **T18** ports `null_bind_group_layouts` (1/30), `immediate_data_size` (8), `device_mismatch` (1/3) + a reusable `mismatchedDevice()` harness helper. **Dawn passes all (reference)**; `null_bind_group_layouts` surfaces **F-020** (yawgpu doesn't implement null BGL slots — rejects them) and **F-021** (wgpu-native aborts on them, `conv.rs:506`, `:*` prefixes). `immediate_data_size` runs **only on yawgpu** (`maxImmediateSize=64`; Dawn/wgpu-native report 0 and skip) and passes — no oracle cross-check, not a finding. `device_mismatch` green on all three. **T21** completes the file with `{create,set}_pipeline_with_null_bind_group_layouts` — introduces the **shader/pipeline/pass foundation** (`createShaderModuleTracked` (WGSL), `create{Render,Compute}PipelineTracked`, `createBindGroupTracked`; reuses `createTextureTracked`/`createViewTracked` + `createCommandEncoderTracked`/`finishTracked`), the base for later operation/shader phases. **Dawn passes both (reference, validating the infra across 32 subcases each)**; surfaces **F-022** (yawgpu rejects `minBindingSize=0` at pipeline creation instead of deferring) and extends **F-021** (wgpu-native aborts on null BGL in pipeline creation/use, `conv.rs:506`). New pipeline-layout resource type; fixture deviation `GpuTest` |
| `api/validation/buffer/mapping.spec.ts` | partial | **24 / 33 — every faithfully-portable test ported** (all `mapAsync,*` incl. OOB/alignment, all runnable `getMappedRange,*` state/subrange/alignment/OOB, `unmap,state,*`) | 7 `.unimplemented` (no native analog): `mapAsync,{earlyRejection,abort_over_invalid_error}`, `{mapAsync,getMappedRange,unmap},state,mappingPending` (JS microtask/promise model), `getMappedRange,disjointRanges{,_many}` (C API doesn't enforce disjoint-range rejection); 2 **N/A** `gc_behavior,*` (JS GC + `ArrayBuffer` detach) | **T19** completes the file. **Dawn + yawgpu pass all 26 cases (identical)**; wgpu-native fails 13 + crashes 7 across `getMappedRange`/`unmap` — same `mapAsync` divergence class ([F-003](FINDINGS.md), 15 `:*` prefixes). New infra: `createMappableBuffer`, `expectGetMappedRange` (null=fail, not a validation error; JS `byteLength`/detach assertions dropped), `kMapModeOptions`/`kOffsetAlignment`/`kSizeAlignment`. `bufferMapSync` + `getErrorBuffer`; fixture deviation `GpuTest` |
| `api/validation/createTexture.spec.ts` | ported | 17 ported, all in-scope (…`usage`, `new_usages`, `texture_usage`, `viewFormats`, `sampleCount,valid_sampleCount_with_other_parameter_varies`) | `texture_size,default_value_and_smallest_size,*` **N/A** (JS array-length defaulting collapses to `{1,1,1}` in C); `depthOrArrayLayers_and_mipLevelCount_for_transient_attachments` + `transient_viewFormats` **out-of-scope** (TRANSIENT_ATTACHMENT — see [F-015](FINDINGS.md) scope note; their non-transient control cases are covered by `viewFormats`) | **Texture file T1–T7 + T12 — COMPLETE (all in-scope tests ported).** On `AllFeaturesMaxLimitsGpuTest`. `texture_format.h` holds all **101** formats + `kRegularTextureFormats` (43) + `multisample`/class/predicates + tier1-blendable set + `maxMipLevelCount` + `textureFormatAndDimensionPossiblyCompatible` + `roundDown`/`getMaxValidTextureSizeForFormatAndDimension` + 28-entry compressed size-variant table; `capability_info.h` adds `kTextureUsages`(6)/`kAllTextureUsages`/`kSomeBogusTextureUsage`/`isValidTextureUsageCombination`; helpers incl. `skipIfTransientAttachmentNotSupported` (skips — `TRANSIENT_ATTACHMENT` is out-of-scope, see [F-015](FINDINGS.md) scope note). Findings: yawgpu rejects 12 color formats + aborts on `Depth24PlusStencil8` ([F-005](FINDINGS.md)) and mis-handles 6 multisample formats ([F-006](FINDINGS.md)); **wgpu-native aborts on bogus + transient usage bits** ([F-007](FINDINGS.md)); **yawgpu under-validates transient usage combos** ([F-008](FINDINGS.md)). Deviations: per-case subcase-expansion (T3); size-variant + transient-usage encodings noted in the file header. `usage` 36, `new_usages` 6. **`texture_usage`** (T6) adds color-renderable (39) / storage (22) / tier1-storage (17) capability tables + `isTextureFormatColorRenderable` / `isTextureFormatUsableAsWriteOnlyStorageTexture`; ~330 cases × 27 subcases, **Dawn passes all 330** (validates the capability model); wgpu-native crashes every compatible case ([F-007](FINDINGS.md), recorded via a `texture_usage:*` **prefix expectation**), yawgpu over-restricts 3D render-attachment + storage ([F-009](FINDINGS.md)). **`viewFormats`** (T7) adds `ParamsBuilder::expand` (per-record value expander, used via the T3b per-case loop) + `textureFormatsAreViewCompatible` (= same `baseFormat`, 23 srgb pairs) + `kFeaturesForFormats`/`filterFormatsByFeature`; 36 cases (feature pairs) × 10201 (=101²) subcases, **Dawn passes all 36**; yawgpu skips 32 feature-gated pairs and the 4 runnable cases fail/crash on the [F-005](FINDINGS.md) format rejections (no clean pass), wgpu-native passes 25 / skips 11. **Update — yawgpu `92db062`:** every yawgpu divergence above (F-005/006/008/009/010) was reported here and **fixed upstream**; yawgpu now **passes all 16 createTexture tests** (0 fail/crash, Metal & Windows/Vulkan). Only the wgpu-native findings (F-007, etc.) remain open. The behaviour notes above describe what the suite originally surfaced — see the [FINDINGS re-test](FINDINGS.md) for the resolution. **`sampleCount,valid_sampleCount_with_other_parameter_varies`** (T12) adds `kValidCombinationsOfOneOrTwoTextureUsages` + `isTextureFormatPossiblyUsableAsColorRenderAttachment` / `isTextureFormatPossiblyStorageReadable` (reuses T6 multisample/storage caps + T11 `skipIfTextureFormatNotUsableAsRenderAttachment`); 330 cases × 17424 subcases, **Dawn passes all 330; yawgpu identical (clean)**; wgpu-native passes 306 / skips 24 feature-gated formats — **no findings**. createTexture is now complete (all in-scope tests ported) |
| `api/validation/createView.spec.ts` | ported | 10 / 10 (`aspect`, `format`, `dimension`, `cube_faces_square`, `texture_state`, `array_layers`, `mip_levels`, `texture_view_usage`, `texture_view_usage_of_multiple_usages`, `texture_view_usage_with_view_format`) | — (complete; one `TRANSIENT_ATTACHMENT` subcase is skipped as out-of-scope, see [F-015](FINDINGS.md) scope note) | **createView file T8–T11 — COMPLETE.** On `AllFeaturesMaxLimitsGpuTest`. `createViewTracked` + `kTextureAspects`(3) + `isDepth/isStencilTextureFormat` (T8); `kTextureViewDimensions`(6) + `getTextureDimensionFromView` + `createTextureWithState` (T9). T8 `aspect` 303 (101×3) + `format` 36/21614 (=101×107×2, reuses T7 view-compat/feature/`expand`) — green on all three. T9 `dimension` 21, `cube_faces_square` 15, `texture_state` 1/3 — **Dawn passes all 37** (reference). yawgpu's F-011 (2D-view-of-multilayer + Cube + CubeArray-square) was **fixed in `41e007b`**. T10 `array_layers`/`mip_levels` add the `reifyTextureViewDescriptor` + `validateCreateViewLayersLevels` range infra (9 cases × 1334/789 subcases; **Dawn passes all**). New findings: **wgpu-native aborts all createView range cases** ([F-013](FINDINGS.md), `array_layers:*`/`mip_levels:*` prefix), also rejects createView on a destroyed texture ([F-012](FINDINGS.md)); **yawgpu under-validates 3D-view array-layer ranges** ([F-014](FINDINGS.md), 2 cases — **fixed in yawgpu `baa78cb`**). **T11** ports the final three view-`usage` tests — `texture_view_usage` (452 cases/2260 subcases), `texture_view_usage_of_multiple_usages` (16/54), `texture_view_usage_with_view_format` (505/51005) — adding `isDepthOrStencilTextureFormat`, `isTextureFormatPossiblyUsableAsRenderAttachment`, `GpuTest::{isTextureFormatUsableAsRenderAttachment,skipIfTextureFormatNotUsableAsRenderAttachment,skipIfTextureFormatDoesNotSupportUsage}` (reuses `kTextureUsages`/`isValidTextureUsageCombination` from T5, view-compat from T7, color-renderable/write-only-storage caps from T6). **Dawn passes all three (reference); yawgpu is identical to Dawn (clean — enforces the view-usage subset rule)**; wgpu-native does not enforce the subset rule ([F-015](FINDINGS.md), 324/452 `texture_view_usage` cases, `texture_view_usage:*` prefix). `TRANSIENT_ATTACHMENT` is treated as out-of-scope (`skipIfTransientAttachmentNotSupported()` now skips). Triaged in `expectations/{wgpu-native,yawgpu}.txt` |
| `api/operation/command_buffer/clearBuffer.spec.ts` | ported | 1 / 1 (`clear`) | — | **T22 — opens `api/operation` (Phase 4).** First execute-and-verify (readback) test. New readback foundation: `makeBufferWithContents` + `expectGPUBufferValuesEqual` (snapshot via copyBufferToBuffer to a `MAP_READ` staging buffer + map + byte `memcmp`), reused by all future operation tests. **Dawn passes (reference)**; **yawgpu now passes all 50 subcases** ([F-023](FINDINGS.md) resolved in `e56f30a` — the 0-size Metal blit-encoder abort plus a follow-on `clearBuffer` zero-fill / `WGPU_WHOLE_SIZE` bug the abort had masked); wgpu-native still aborts the `size=0` subcase via [F-002](FINDINGS.md) (`src/lib.rs:1294`), contained via `--isolate` + `:*` prefix. Fixture `GpuTest` |
| `api/operation/command_buffer/copyBufferToBuffer.spec.ts` | ported | 3 / 3 (`single`, `state_transitions`, `copy_order`) | — | **T22.** Copy + readback verification. **Dawn passes all (reference)**; `state_transitions`/`copy_order` green on all three; `single` (which has a `copySize=0` subcase) now passes on **yawgpu** too ([F-023](FINDINGS.md) resolved in `e56f30a`) and on wgpu-native. Reuses the T22 readback foundation. Fixture `GpuTest`. **Deviation (`single`, invalid subcases):** upstream wraps `submit(finish())` in `expectValidationError(…, !isValid)` uniformly; the C port instead expects the error at **`finish()`** for invalid cases and does **not** submit the invalid command buffer (valid cases still expect at `submit`). Equivalent coverage (invalid copy → validation error + `dst` unchanged); chosen so backends that *abort* on submitting an invalid command buffer don't crash the case. Reviewed 2026-06-02 (Phase 4 fidelity audit) — kept deliberately. |
| `api/operation/queue/writeBuffer.spec.ts` | ported | 2 / 2 (`array_types` 16 cases, `multiple_writes_at_different_offsets_and_sizes` 8 subcases) | — | **T23.** `queue.writeBuffer` upload foundation. C has **no TypedArray** — the 8 JS typed arrays are modelled as an explicit little-endian byte encoder (ints two's-complement, floats IEEE-754), with `dataOffset`/`dataSize` resolved to byte ranges before `wgpuQueueWriteBuffer`; verified via the T22 readback. Encoder has 8 `cts_unittests` byte-assertions. **Pass=17/17 on all three backends** (yawgpu/Dawn/wgpu-native) — no findings. Reuses `expectGPUBufferValuesEqual`. Fixture `GpuTest` |
| `api/operation/command_buffer/basic.spec.ts` | ported | 3 / 3 (`empty`, `b2t2b`, `b2t2t2b`) | — | **T23 — texture-copy foundation.** Adds `copyBufferToTexture`/`copyTextureToBuffer`/`copyTextureToTexture` helpers (the basis for all later texture readback). `empty` green on all three. `b2t2b`/`b2t2t2b` roundtrip a u32 through a `1×1×1 rgba8uint` texture: **green on all three** (return `0x01020304`). yawgpu initially read back all-zeros — its HAL was missing `rgba8uint` ([F-024](FINDINGS.md), resolved in `c893eac` by adding `rgba8uint` then expanding HAL coverage to all uncompressed color formats); `expectations/yawgpu.txt` now has no entry. Fixture `GpuTest` |
| `api/operation/onSubmittedWorkDone.spec.ts` | ported | 5 / 5 (`without_work`, `with_work`, `many,serial`, `many,parallel`, `many,parallel_order`) | — | **T23.** `queue.onSubmittedWorkDone` as a sync primitive: `onSubmittedWorkDoneSync` (single) + `onSubmittedWorkDoneMany`/`processEventsUntil` (issue N callbacks, pump until all fire; `parallel_order` asserts submission-order resolution). **Pass=5/5 on all three backends** — no findings. Fixture `GpuTest` |
| `api/operation/command_buffer/image_copy.spec.ts` | partial (color + depth/stencil) | 7 / 7 (5 color: `rowsPerImage_and_bytesPerRow`, `offsets_and_sizes`, `origins_and_extents`, `mip_levels`, `undefined_params`; 2 depth/stencil: `rowsPerImage_and_bytesPerRow_depth_stencil`, `offsets_and_sizes_copy_depth_stencil`) | compressed-format slices deferred | **T24a/T24b — the TexelView comparison foundation + the color `image_copy` port (the largest single slice so far: 137256 subcases).** **T24a** ports the upstream TexelView stack: `util/texel_data` (`kTexelRepresentationInfo` pack/unpack + decode for 43 uncompressed color formats — unorm/snorm/uint/sint/float16/rg11b10ufloat/rgb9e5/bgra/srgb), `util/texel_view` (`TexelView::fromTextureDataByReference`), `util/texture_ok` (`findFailedPixels` with `maxFractionalDiff`), `util/texture_layout` (`getTextureCopyLayout`/`bytesInACompleteRow`/`updateLinearTextureDataSubBox`), and the harness check `expectGPUBufferValuesEqualWhenInterpretedAsTextureFormat` (decode-value comparison, **not** byte-`memcmp`). **T24b** ports the 5 color tests, each = `format × dimension × initMethod × checkMethod` cases × per-test subcase sweeps (`rowsPerImage_and_bytesPerRow` padding×copySize; `offsets_and_sizes` offsets×depth×width×rowsPerImage; `origins_and_extents` origin×copySize×texturePadding×coordinate; `mip_levels` textureSize×mipOffset; `undefined_params` undefined bytesPerRow/rowsPerImage/origin) — `initMethod ∈ {WriteTexture, CopyB2T}`, `checkMethod ∈ {FullCopyT2B, PartialCopyT2B}`; adds `queue.writeTexture` (`wgpuQueueWriteTexture`). The `FullCopyT2B` helper mirrors upstream exactly (snapshot the whole subresource → overlay the uploaded sub-box on the CPU → re-read the whole subresource → compare; **no** `queueWriteTexture` prefill). **Dawn passes all — `pass=137256 fail=0` (oracle; validates the whole port and the faithful `FullCopyT2B`).** Findings: yawgpu surfaced **F-025** (`queueWriteTexture` writes zeros — every WriteTexture-init case) and **F-026** (`copyBufferToTexture`/`copyTextureToBuffer` mishandle non-default buffer layout + mip levels), both **RESOLVED** in `1e6c70b` (HAL texture dimension/array/mip + queueWriteTexture upload) → full yawgpu run now `pass=137256 fail=0` (Dawn-equal; never masked, `expectations/yawgpu.txt` stays empty); wgpu-native surfaces **F-027** (3D whole-subresource readback after a non-zero-origin copy — `origins_and_extents` 3D `FullCopyT2B`; `PartialCopyT2B` + the other 3D-Full tests pass) → `pass=116772 skip=19152 fail=1332` (37 cases × 36 subcases). F-027 is left **surfaced/unmasked** (the failing cases are partial — 36/144 subcases — and the harness's case-level expectations can't mask them without xpass noise; see [F-027](FINDINGS.md)). Later **combined-run** (whole-listing fast-mode) testing on Windows/Vulkan surfaced two **cross-test** yawgpu findings, both **RESOLVED** and re-verified on Metal: **F-029** (image_copy leaked Vulkan device resources, poisoning every later GPU test in the same process — fixed `1e67300`) and **F-030** (a `MAP_READ` readback race the F-029 fix un-masked — fixed `1297b7e`); `image_copy:*` is now deterministically `137256/0` and a combined `api,operation` run is poison-free. **T27** adds the 2 depth/stencil tests (`rowsPerImage_and_bytesPerRow_depth_stencil` 12 cases/864 subcases; `offsets_and_sizes_copy_depth_stencil` 12/288): the stencil aspect via `WriteTexture`/`CopyB2T`/`CopyT2B` and the depth aspect via `CopyT2B`, adding the `r32float`→`@builtin(frag_depth)` depth-staging render path and `depthStencilBufferTextureCopySupported`. **Dawn, yawgpu, and wgpu-native all pass 1152.** yawgpu initially zeroed the depth aspect of `copyTextureToBuffer` and the stencil aspect of packed depth+stencil formats (**F-032**, `pass=288 fail=864`; plain `Stencil8` passed), **resolved** in yawgpu `c8f15d5`/`af9ac5c` (depth/stencil aspect buffer-copy support + packed-aspect buffer-size validation); re-test `pass=1152 fail=0`, surfaced/unmasked (see [F-032](FINDINGS.md)). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/command_buffer/render/state_tracking.spec.ts` | **ported** | 5 / 5 (all `set_*`/`change_pipeline_*` tests) | — (complete) | **T69 (V26d) + T70 (V26d-rest) — render index/vertex-buffer state tracking; opens & completes `command_buffer/render/state_tracking`.** Re-setting `setIndexBuffer`/`setVertexBuffer` (format/offset/size) and swapping the pipeline across draws in one render pass must keep each draw correct. The 5 tests: `set_index_buffer_without_changing_buffer` (4 draws, varying Uint32/Uint16 + offset/size, incl. uint16-bytes-as-uint32 `0x10000` index), `set_vertex_buffer_without_changing_buffer` (offset/size over 8 verts), `change_pipeline_before_and_after_vertex_buffer` (two strides 8/16; pipeline-carried stride), `set_vertex_buffer_but_not_used_in_draw` (a bound-but-unused slot; 2 custom pipelines), `set_index_buffer_before_non_indexed_draw` (index binding must not affect a non-indexed draw). Reuses point-list vertex/index draws + `copyTextureToBuffer` per-pixel byte check — **no new harness** (buffers padded to 4-aligned `makeBufferWithContents`). **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass** — **no findings**. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/command_buffer/render/render_bundle.spec.ts` | ported | 4 / 4 (`basic`, `two_bundles`, `one_bundle_used_multiple_times`, `one_bundle_used_multiple_times_same_executeBundles`) | — | **T54 (V25) — render-bundle execution; opens `command_buffer/render`.** Record draws into a `GPURenderBundle` (`createRenderBundleEncoder` → `finish`) and `executeBundles` them; verify the result matches direct draws. Covers a basic bundle, two bundles (instance/vertex offsets → bottom-left `kColor0` / top-right `kColor1`), one bundle reused 4× with `setViewport`-selected `1×1` regions, and one bundle replayed 3× in a single `executeBundles` with **additive blend** (→ `kColor0×3`). Reuses the V7b render-bundle + T45 `setViewport` + V4 blend + a `4×4` per-pixel readback — **no new harness**. **Dawn and wgpu-native pass all 4.** Finding: **F-049** — yawgpu passes only `basic`; the other 3 fail (`pass=1 fail=3`): the **viewport rect is ignored** (the reused-bundle test fills the whole target), and the two/blended-bundle cases mis-apply bundle draw-args / blend / repeated replay; **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `f82c2d6` (viewport/scissor threaded through the subpass path) — re-test `pass=4 fail=0` on both HALs (was `pass=1 fail=3`), surfaced/unmasked. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/command_buffer/programmable/state_tracking.spec.ts` | partial (compute bind-group indices) | 1 / 6 (`bind_group_indices`, compute/storage) | `bind_group_{order, before_pipeline, multiple_sets}`, `one_bind_group_multiple_slots`, `compatible_pipelines` + render-pass/uniform (deferred V26b/c) | **T55 (V26) — bind-group-index state tracking; opens `command_buffer/programmable`.** Bind groups can be set at any group index (in any order) regardless of their shader binding order. Ports `bind_group_indices` for **compute pass / storage**: a compute shader reads `a` (=3) and `b` (=2) at permuted group indices and writes `a-b=1` to `out`; per-subcase generated WGSL + an explicit 3-slot pipeline layout; 6 index permutations. Reuses the V2 compute + storage readback — **no new harness**. **Dawn, yawgpu, wgpu-native all pass 6** — **no findings**. Deferred: render-pass/render-bundle encoder types + uniform (V26b), the other 5 tests (V26c), `command_buffer/render/state_tracking` vertex/index-buffer state (V26d). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/command_buffer/render/dynamic_state.spec.ts` | N/A (upstream stub) | 0 / 0 | — | Upstream is a **TODO stub** (no `g.test` — viewport/scissor/blend/reference dynamic-state tests are an unwritten upstream TODO); **nothing to port** until upstream adds tests. |
| `api/operation/command_buffer/queries/occlusionQuery.spec.ts` | partial (minimal) | 4 / 11 (`occlusion_query,{basic, empty, depth, stencil}`) | `scissor`, `sample_mask`, `alpha_to_coverage`, `reuse`, `initial`, `unsubmitted_render_pass`, `multi_resolve` + the `kQueryTestBaseParams` matrix (deferred V30b-rest) | **T58 (V30) — occlusion queries; opens `command_buffer/queries`.** An occlusion `GPUQuerySet` + `beginOcclusionQuery`/`endOcclusionQuery` brackets a render-pass draw; `resolveQuerySet` writes the `u64` sample count. Minimal slice: `basic` (a covering triangle → result `!= 0`) + `empty` (no draw → result `== 0`). Reuses the V1 render pass + a `u64` buffer readback — **no new harness**. **Dawn and wgpu-native pass both.** Finding: **F-050** — yawgpu's occlusion query resolves to **`0`** even for a covering draw (the passing samples are never counted; `basic` fails, `empty` coincidentally passes), `pass=1 fail=1`, **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `37d36e6`+`e70d18d` (occlusion-query execution on Metal + Vulkan) — re-test `pass=2 fail=0` on both HALs, surfaced/unmasked. **T71 (V30b)** adds `occlusion_query,depth` + `occlusion_query,stencil` (minimal, single fixed cases): a render pass with **two** occlusion queries over a `depth24plus-stencil8` attachment brackets an occluded draw (depth-fail `0.9<0.5` / stencil-fail `ref 1 == clear 0`) → resolved `== 0`, and a passing draw → resolved `!= 0`; the depth case sets `z` via a WGSL `override` (`WGPUConstantEntry`). **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass** — **no findings**. Deferred **V30b-rest**: `scissor`/`sample_mask`/`alpha_to_coverage`/`reuse`/`initial`/`unsubmitted_render_pass`/`multi_resolve` + the `kQueryTestBaseParams` matrix. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/command_buffer/queries/timestampQuery.spec.ts` | partial (minimal) | 1 / 5 (`resolve_unused_slots`) | `many_query_sets`, `many_slots`, `multi_resolve`, `unused_slots_are_zero` (deferred V30c-rest) | **T75 (V30c) — timestamp queries; opens `command_buffer/queries/timestampQuery`.** Feature-gated (`timestamp-query`). Since the resolved values are implementation-defined timing numbers, the deterministic property tested is that **unused (unwritten) slots resolve to 0**: a `timestamp` querySet (count 2) has a `compute`/`render` pass with `timestampWrites` encoded into a command buffer that is **finished but never submitted**, then a separate encoder `resolveQuerySet`s the slots → the buffer must be all-zero. Reuses the V30 querySet/resolve + `u64` readback — **no new harness**. **Dawn, yawgpu (Metal + MoltenVK) pass (2 subcases each); wgpu-native skips** (its adapter lacks `timestamp-query`) — **no findings**. Deferred **V30c-rest**: `many_query_sets`, `many_slots` (4k), `multi_resolve`, `unused_slots_are_zero`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/compute_pipeline/overrides.spec.ts` | partial (sync) | 1 / 7 (`basic`, isAsync=false) | `numeric_id`, `computed`, `precision`, `workgroup_size`, `shared_shader_module`, `multi_entry_points`, isAsync=true (deferred V27b/c) | **T56 (V27) — compute override constants; opens `compute_pipeline`.** 11 `override` constants (bool/f32/i32/u32, some pipeline-set via `WGPUConstantEntry`, some WGSL-defaulted) feed a compute shader that writes `u32(c_i)` to a storage buffer; verify `[0..10]`. The compute analog of the render `overrides` port (T50/F-047). Reuses the V2 compute + storage readback — **no new harness**. **Dawn and wgpu-native pass.** Finding: **F-047** (the same override-constant gap) reproduced on the **compute** stage — a pipeline-provided `override` read back `0` (`pass=0 fail=1`), **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `fff8634` (overridable constants applied in shader codegen, render + compute) — re-test `pass=1 fail=0` on both HALs, surfaced/unmasked. Deferred V27b (isAsync) / V27c (the other 6 tests). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/compute_pipeline/entry_point_name.spec.ts` | N/A (upstream stub) | 0 / 0 | — | Upstream is a **TODO stub** (no `g.test` — entry-point-name tests are an unwritten upstream TODO); **nothing to port** until upstream adds tests. |
| `api/operation/command_buffer/copyTextureToTexture.spec.ts` | partial (color + depth/stencil) | 4 / 9 (`color_textures,non_compressed,non_array`, `color_textures,non_compressed,array`, `zero_sized`, `copy_depth_stencil`) | `color_textures,compressed,{non_array,array}`, `copy_multisampled_{color,depth,stencil}` (registered `unimplemented`) | **T25 + T26.** **T25** — color (non-compressed) `copyTextureToTexture` data-correctness across all regular color formats × dimensions × 2D-array/3D layers × mip levels × copy-box offsets (`non_array` 141 cases/11280 subcases; `array` 94/19552; `zero_sized` 1/78). Reuses the T23 `copyTextureToTexture`/`copyTextureToBuffer` helpers + the T24a TexelView decode-value comparison (`findFailedPixels` `maxFractionalDiff=0`) + `queueWriteTexture`; adds `physicalMipSize`. Format axes are `srcFormat`/`dstFormat` (also added to the `--sample-formats` registrar). **Dawn (oracle) and yawgpu both pass `pass=30910 skip=6 fail=0` (identical, clean).** wgpu-native surfaces **F-028** (3D depth slices z≥1 read back zero — `array;dimension=3`; the 2D-array and `non_array` cases pass) → `pass=26236 skip=3942 fail=738` (41 cases × 18 subcases), surfaced/unmasked. Deviation: the copy-box offset/extent clamp uses per-texture remaining size rather than upstream's `minWidth − max(offset)` — valid, self-consistent (Dawn-verified). **T26** — `copy_depth_stencil` (6 cases/216 subcases over `kDepthStencilFormats`), which stands up the **depth/stencil verification foundation**: aspect-aware stencil `writeTexture`/`copyTextureToBuffer` + byte compare, and a depth-compare render foundation (per-layer render passes via raw C-API, dynamic-offset uniform, `depthCompare` init `always`/verify `equal`, `expectSingleColor` green readback). **Dawn, yawgpu, and wgpu-native all pass 216 (`pass=216 fail=0`).** yawgpu's depth aspect initially diverged (**F-031** — every depth format failed the equal re-render; `Stencil8` stencil-only passed; `pass=36 fail=180`), **resolved** in yawgpu `f3afc31` by adding depth support to its regular real-backend render path + multi-layer depth copies; re-test `pass=216 fail=0` (Dawn-equal), surfaced/unmasked (see [F-031](FINDINGS.md)). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/basic.spec.ts` | partial (color-render foundation) | 2 / 3 (`clear`, `fullscreen_quad`) | `large_draw` (registered `unimplemented`) | **T28 (V1) — the color render-to-texture foundation, opens the `rendering` area.** First **color** render path (T26/T27 built only the *depth* path): a render pass with a 1×1 `rgba8unorm` color attachment (`clearValue`/`loadOp=clear`/`storeOp=store`) and, for `fullscreen_quad`, an empty-layout render pipeline (verbatim fullscreen-triangle vertex + green `@location(0)` fragment, `triangle-list`) + `draw(3)`; read back via `copyTextureToBuffer` + `expectGPUBufferValuesEqual` (`clear`→green; `fullscreen_quad` clears red then the quad overwrites green). Reuses the T26 raw-C-API render-pass pattern + `gpu.h` creators — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass (`pass=2 skip=1 fail=0 crash=0`; `large_draw` skipped)** — no findings. `large_draw` deferred (index buffers + `drawIndexed`/`drawIndirect`/instancing + a time-limit loop + check-function readback). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/compute/basic.spec.ts` | partial (compute foundation) | 1 / 2 (`memcpy`) | `large_dispatch` (registered `unimplemented`) | **T29 (V2) — the compute foundation, opens the `compute` area.** The suite's first **compute** pipeline (all prior operation tests are copy/render): a `@compute @workgroup_size(1)` shader copies one `u32` from a `read` storage buffer to a `read_write` storage buffer; dispatched in a raw-C-API compute pass (`beginComputePass`/`setPipeline`/`setBindGroup`/`dispatchWorkgroups(1,1,1)`/`end`) and read back via `expectGPUBufferValuesEqual` (`dst == 0x01020304`). Explicit `COMPUTE` storage BGL (binding 0 read-only, binding 1 read_write). Reuses `gpu.h` creators + the compute-pass pattern from `createPipelineLayout.spec.cpp` — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass (`pass=1 skip=1 fail=0 crash=0`; `large_dispatch` skipped)** — no findings. `large_dispatch` deferred (device limits + dynamic shader codegen + check-function readback). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/draw.spec.ts` | partial (draw-call foundation) | 2 / 5 (`arguments`, `default_arguments`) | `vertex_attributes,basic`, `vertex_attributes,formats`, `largeish_buffer` (registered `unimplemented`) | **T30 (V3) — the draw-call foundation.** Ports the shared `checkTriangleDraw` (a unit triangle rendered into a 6×6 grid on a 72×36 `rgba8unorm` target — primitive id along X, instance id along Y — selected by the draw args) + `arguments` (720 cases) and `default_arguments` (2 cases/24 subcases): **vertex & index buffers**, all four draw variants (`draw`/`drawIndexed`/`drawIndirect`/`drawIndexedIndirect`), instancing, `baseVertex`/`firstIndex`/`firstInstance`, buffer offsets, a fragment `read_write` storage write, and **per-pixel coordinate** verification (a new local `expectSinglePixelComparisonsAreOkInTexture` over the existing `expectGPUBufferValuesPassCheck`). Reuses the T28 color-render + T26 raw-C-API render-pass patterns — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass 744.** yawgpu initially didn't execute **indexed/indirect** draws at all (**F-034** — `drawIndexed`/`drawIndirect`/`drawIndexedIndirect` weren't issued, so the fragment storage `result` read 0; plain `draw` passed) → `pass=340 fail=224`, **resolved** in yawgpu `36a6b66` (indexed/indirect draw execution); re-test `pass=564 fail=0` (Dawn/wgpu-equal, +183 `indirect-first-instance` skips), surfaced/unmasked (see [F-034](FINDINGS.md)). `vertex_attributes,*` (vertex-format machinery) + `largeish_buffer` (stress) deferred. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/color_target_state.spec.ts` | partial (blend-state foundation) | 5 / 8 (`color_write_mask,{channel_work,blending_disabled}`, `blend_constant,{initial,setting,not_inherited}`) | `blending,{GPUBlendComponent,formats,clamping}` (registered `unimplemented`) | **T31 (V4) — the color-target blend-state foundation.** First `GPUColorTargetState` `blend` + `writeMask` + `setBlendConstant` coverage (all `rgba8unorm`, 1×1): a render pipeline built from a given `WGPUColorTargetState` (verbatim fullscreen-triangle vertex + uniform-`params.color` fragment, explicit FRAGMENT uniform BGL), `color_write_mask,channel_work` (`kColorWriteCombinations`, 16 cases), `color_write_mask,blending_disabled` (default `WGPU_BLEND_STATE_INIT` blend == disabled), `blend_constant,{initial,setting,not_inherited}` (`srcFactor=constant`, two-pass not-inherited). Local `expectColorOkInTexture` (`copyTextureToBuffer` + per-channel `byte/255` compare, `maxDiff=0` for 0/1, `1.5/255` for the `0.5` subcase) over `expectGPUBufferValuesPassCheck` — **no new harness**. **Dawn (oracle) passes all 23 (`pass=23 fail=0`).** Findings: **F-035** — yawgpu **ignored both `blend` and `writeMask`** (wrote the raw fragment output to all channels), failing 21/23 (`pass=2 fail=21`; only the all-channels/`constant=1` cases passed), **cross-HAL** (Metal == Vulkan/MoltenVK byte-identical, **not** a MoltenVK artifact), now **RESOLVED** in yawgpu `74f5ef2` (apply color-target blend + writeMask + blend constant) — re-test `pass=23 fail=0` on **both** Metal and Vulkan/MoltenVK (Dawn-equal), surfaced/unmasked; **F-036** — wgpu-native **aborts** (`signal 6`) on `blend_constant,{initial,not_inherited}` (a constant-factor blend drawn without `setBlendConstant`, which should default to `[0,0,0,0]`), contained via `--isolate` + `expectations/wgpu-native.txt` (`color_write_mask,*` + `blend_constant,setting` pass on wgpu-native). Deferred to **V4b**: `blending,{GPUBlendComponent,formats,clamping}` (the `GPUBlendFactor`×`GPUBlendOperation` matrix + CPU blend math + dual-source + rgba16float tolerance). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/depth.spec.ts` | ported | 5 / 5 (`depth_disabled`, `depth_write_disabled`, `depth_test_fail`, `depth_compare_func`, `reverse_depth`) | — | **T32 (V5) — the depth-test rendering foundation.** First standalone depth-stencil-**attachment** render path: a render pass with a `1×1 rgba8unorm` color attachment **and** a depth-stencil attachment, a `WGPUDepthStencilState` (`format` + `depthWriteEnabled` [`WGPUOptionalBool`] + `depthCompare` + stencil sub-state), depth fed through the vertex shader `z` (baked per pipeline), `point-list`. `runDepthStateTest` multi-draw "which point wins" helper (depth24plus-stencil8, `load`); `depth_compare_func` sweeps `format × depthCompare × depthClearValue` (`kDepthTextureFormats`, 120 cases) with the expected computed via a CPU `comparePasses(0.4, clearValue)` (equivalent to upstream's 24-row table); `reverse_depth` uses reversed-Z + 4 instanced points; out-of-range depth (`2.0`) clipping covered in `depth_test_fail`. Reuses the T26 depth render pattern + the T31 rgba8unorm `expectColorOkInTexture` — **no new harness**. **Dawn (oracle), wgpu-native (Metal), and yawgpu's own Vulkan/MoltenVK HAL all pass `130/130` deterministically.** Finding: **F-037** — yawgpu's **Metal HAL** flaked **non-deterministically** (`fail≈33–44/130`, varying run-to-run; the drawn point's output intermittently read back as the clear value) while every case passed in isolation and Vulkan/MoltenVK/Dawn/wgpu-native-Metal were all clean — now **RESOLVED** in yawgpu `186cd54`: the root cause was the Metal HAL not emitting `[[point_size]]` for **`point-list`** pipelines (these depth tests are the suite's first point-list users), so point size was undefined on Metal; re-test `pass=130 fail=0` across 11 runs, neighboring triangle-list rendering unaffected. Surfaced/unmasked. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/stencil.spec.ts` | partial (stencil-test foundation) | 6 / 7 (`stencil_compare_func`, `stencil_passOp_operation`, `stencil_failOp_operation`, `stencil_depthFailOp_operation`, `stencil_read_write_mask`, `stencil_reference_initialized`) | `stencil_accumulation` (registered `unimplemented`) | **T33 (V5b) — the stencil-test rendering foundation.** Extends the T32 depth path to stencil: `wgpuRenderPassEncoderSetStencilReference` + the `WGPUStencilFaceState` (`compare`/`failOp`/`passOp`/`depthFailOp`) + `stencilReadMask`/`stencilWriteMask`, over `kStencilTextureFormats` (`stencil8`, `depth24plus-stencil8`, `depth32float-stencil8`; stencil-only `stencil8` handled — depth ops gated on `isDepthTextureFormat`). The 6 tests verify stencil state via a **color** readback (green if the stencil test passes / the resulting stencil equals the expected value; `checkStencilOperation`/`checkStencilCompareFunction` builders + `runStencilStateTest`, incl. the single-encoder-multi-pass `stencil_reference_initialized`). Reuses the T32 point-list + depth-stencil-attachment + `expectColorOkInTexture` foundation — **no new harness**. **Dawn (oracle) and wgpu-native (Metal) both pass all 188 (`pass=188 fail=0`).** Finding: **F-038** — yawgpu **mishandled stencil ops/compare/masks** (`pass=97 fail=91` deterministic, **cross-HAL** Metal == Vulkan/MoltenVK byte-identical), now **RESOLVED** in yawgpu `40f5d7f`: the single root cause was the dynamic stencil reference (`setStencilReference`) not being threaded to the HAL, scrambling everything downstream of it; re-test `pass=188 fail=0` on **both** HALs (Dawn/wgpu-equal), neighboring rendering+compute `pass=720 fail=0`. Surfaced/unmasked. Deferred to **V5c**: `stencil_accumulation` (per-bit additive-blend renders + stencil-aspect `copyTextureToBuffer` readback + `DataArrayGenerator` + mip levels). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/depth_bias.spec.ts` | ported | 2 / 2 (`depth_bias`, `depth_bias_24bit_format`) | — | **T40 (V10) + T44 (V14) — the depth-bias (polygon-offset) render path.** A quad (flat `z=0.25` / tilted `0→0.5`) drawn into a `1×1` depth texture with a `depthBias`/`depthBiasSlopeScale`/`depthBiasClamp` pipeline state. **T40** ports `depth_bias` (7 cases) on `depth32float`, reading back the depth aspect (raw `WGPUTextureAspect_DepthOnly` `copyTextureToBuffer`) vs the expected biased depth (tol `1/256`). **T44** ports `depth_bias_24bit_format` (`depth24plus`/`depth24plus-stencil8`, 6 cases): 24-bit depth can't be read as float, so it uses the `depthCompare:'greater'` re-render trick (`depthClearValue=0.4`, a red fragment draws iff the biased depth > 0.4), verified via a `1×1 rgba8unorm` **color** readback (red drawn / transparent clamped). Reuses the T32 depth-stencil-attachment + T27 depth-aspect readback — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass 13** — **no findings** (yawgpu handles polygon offset + 24-bit depth bias correctly). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/depth_clip_clamp.spec.ts` | partial (depth32float) | 2 / 2 (`depth_test_input_clamped`, `depth_clamp_and_clip`) | the `unclippedDepth {undefined,true}` + MS + depth-format breadth (deferred V15b/c-rest) | **T45 (V15) — the viewport depth-clamping path.** First `setViewport(...,minDepth,maxDepth)` + `@builtin(frag_depth)` + the `depth-clip-control` (`unclippedDepth`) flag. Ports `depth_test_input_clamped` (`depth32float`, non-MS, `unclippedDepth∈{false,true}` = 2 cases): an init pass writes `clamp(kDepths,0.25,0.75)` into the depth texture; a test pass re-renders unclamped `frag_depth=kDepths` with viewport depth `[0.25,0.75]` + `depthCompare:not-equal` → a correctly-clamped `frag_depth` equals the stored value so nothing draws (the `r8unorm` target stays 0). Reuses the T32 depth-stencil render pass — **no new harness** (`unclippedDepth` is a direct `WGPUPrimitiveState` field; `WGPUPrimitiveDepthClipControl` does not exist in webgpu-headers 1.0). **Dawn (oracle) passes both.** Finding: **F-045** — yawgpu **and wgpu-native** draw (got `255`) because **`frag_depth` is not clamped to the viewport depth range** before the depth test (Dawn clamps); on yawgpu it is **cross-HAL** (Metal == Vulkan/MoltenVK); `unclippedDepth=true` is skipped on yawgpu/wgpu (no `depth-clip-control`), runs only on Dawn — now **RESOLVED for yawgpu** in `155a854` (clamp `frag_depth` to the viewport range): passes on **Metal + native Vulkan** (user-confirmed full-CTS-green on native Windows/NVIDIA). The residual MoltenVK `fail=1` is a **confirmed MoltenVK-only artifact** (Vulkan→Metal `frag_depth` clamp), like F-033 — not a yawgpu defect. **wgpu-native still affected** (separate), surfaced/unmasked. **T66 (V15c)** adds `depth_clamp_and_clip` (minimal: `depth32float`, `unclippedDepth=false`, non-MS, `writeDepth∈{false,true}` = 2 cases) — 64 points (8 vertex-depth × 8 frag-depth), viewport depth `[0.25,0.75]`; a test pass writes each fragment's `position.z` error to a storage buffer + (writeDepth) `frag_depth`, a check pass re-renders at z=0.5 with `depthCompare:not-equal` → `r8unorm` 0/1. **Dawn, yawgpu (Metal + MoltenVK) pass both**; **wgpu-native fails `writeDepth=true`** — the same **F-045** (`frag_depth` not viewport-clamped) it is still affected by (Dawn/yawgpu clamp). Deferred to **V15b** (format × MS matrix) / **V15c-rest** (`unclippedDepth {undefined,true}` feature path, `multisampled=true`). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/robust_access_index.spec.ts` | N/A (upstream stub) | 0 / 0 | — | Upstream is a **TODO stub** (`export const description = 'TODO: Test that drawIndexedIndirect accesses the index buffer robustly.'`, no `g.test`) — **nothing to port** until upstream adds tests. Tracked here so it is not mistaken for un-ported work. |
| `api/operation/vertex_state/correctness.spec.ts` | partial (representative formats) | 1 / 8 (`vertex_format_to_shader_format_conversion`) | the other 7 tests (deferred) | **T46 (V16) — the vertex-format decode path.** Verifies the GPU converts raw vertex-buffer bytes of a given `WGPUVertexFormat` into the shader's `vec4<f32\|u32\|i32>`. Ports `vertex_format_to_shader_format_conversion` over **9 representative formats** (one per decode family: `float32x4, float16x4, uint32x4, uint8x4, sint32x4, sint8x4, unorm8x4, snorm8x4, unorm10_10_10_2`), with **hand-computed expected values** (no generic `kVertexFormatInfo` machinery): known raw bytes → vertex shader reads `vec4<T>` → fragment writes to a zero-init `read_write` storage buffer → compared to the expected decode (exact for int/`f32`; tol for `unorm`/`snorm`/`f16`). Reuses the V7b render-stage storage write + V3 vertex buffer layout — **no new harness**. **Dawn (oracle) and wgpu-native both pass all 9.** Finding: **F-044** — yawgpu **decodes only `float32x4`**; the other 8 formats read back **all zeros** (`pass=1 fail=8`), i.e. vertex-format *conversion* (8/16-bit, packed, normalized, integer, half) is not applied; **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `706087f` (full vertex-format set) — re-test `pass=9 fail=0` on both HALs (was `pass=1 fail=8`), surfaced/unmasked. Deferred to **V16b** (full `kVertexFormats` matrix + component-count/slot/location subcases) / **V16c** (the other 7 `correctness` tests). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pipeline/culling_tests.spec.ts` | partial (no-depth-stencil) | 1 / 1 (`culling`, depthStencilFormat=null) | the `depthStencilFormat` variants (deferred V17b) | **T47 (V17) — face culling / winding; opens the `render_pipeline` area.** Two opposite-winding triangles into a `4×4 rgba8unorm` target (clear blue); the fragment colors by `@builtin(front_facing)` (green=front, red=back); `frontFace` + `cullMode` cull a face (→ corner stays blue). `frontFace{ccw,cw}` × `cullMode{none,front,back}` (6 cases) × `topology{list,strip}` (2 subcases); corner-pixel check. Reuses the V1/V3 color render pass — **no new harness**. **Dawn (12) and wgpu-native (6 cases) pass.** Finding: **F-046** — yawgpu **mishandles `front_facing`/winding** (the CCW triangle reads red when `frontFace=ccw`; cull decisions also wrong), `pass=2 fail=10`, **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `f82c2d6`+`d6e700a` (cull/frontFace threaded through the subpass path) — re-test `pass=12 fail=0` on both HALs, surfaced/unmasked. Deferred **V17b**: the depth/stencil variants (the 2nd "yellow" `depthCompare:less` draw + the 3rd "cyan" stencil draw). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pipeline/primitive_topology.spec.ts` | **ported** | 2 / 2 (`basic` full matrix + `unaligned_vertex_count`) | — (complete) | **T48 (V18) — primitive-topology rasterization.** Draw 6 fixed vertices with each `GPUPrimitiveTopology` into a `56×56 rgba8unorm` target (clear black, green fragment); check valid (covered=green) + invalid (uncovered=black) locations (midpoints/centroids per topology). Reuses the V3 vertex buffer + per-pixel readback — **no new harness**. **T63 (V18b)** extends `basic` to the full `topology × indirect{false,true} × primitiveRestart{false,true}` matrix (**14 cases**; restart strip-only) — adds the indirect-draw (`drawIndirect`/`drawIndexedIndirect`, INDIRECT args `{6,1,0,0}`/`{7,1,0,0,0}`) and primitive-restart (uint32 `0xFFFFFFFF` index in `[0,1,2,-1,3,4,5]`, `drawIndexed(7)`) paths. **Dawn, yawgpu (Metal), wgpu-native all pass 14** — **no findings**. Deferred **V18c**: `unaligned_vertex_count`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pipeline/pipeline_output_targets.spec.ts` | partial (rgba8unorm core) | 3 / 3 (all `color,*` tests) | the format/attachmentCount/no-alpha breadth (deferred V19b/c-rest/d-rest) | **T49 (V19) — fragment-output component-count → color target.** A fragment outputs `vecN<f32>` of `[0,1,0,1]`; components beyond the target's are discarded. `format{r8unorm,rg8unorm,rgba8unorm}` × valid `componentCount` = 3 cases / 8 subcases; `1×1` per-format byte readback. **Dawn, yawgpu, wgpu-native all pass 8** — no findings. **T65 (V19c)** adds `color,attachments` (minimal: rgba8unorm, `attachmentCount=2`, `emptyAttachmentId∈{0,1}` = 2 cases) — a **sparse / null** color-attachment slot (null pipeline target `format=Undefined` + null pass attachment `view=nullptr`) with the other slot written; verify the non-null attachment's 1×1 texel. **Dawn + wgpu-native pass.** Finding: **F-054** — yawgpu **rendered nothing** to the non-null attachment (read `0`; `pass=0 fail=2`), **cross-HAL** (Metal == MoltenVK), now **RESOLVED** (yawgpu `793fc6d`-era; re-test `pass=2 fail=0` on both HALs). **T67 (V19d)** adds `color,component_count,blend` (rgba8unorm, the 5 has-alpha blend-factor subcases: output `vec4f(0,1,0,0.498)`, load `[1,0,0,1]`, `add` op with one/src-alpha/dst-alpha factor combos) — verify the blended 1×1 texel. **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass 5** — **no findings** (the F-035 blend-factor path is solid). Deferred **V19b** (format matrix + sint/uint) / **V19c-rest** (`attachmentCount {3,4}`) / **V19d-rest** (`r8unorm`/`rg8unorm`/`bgra8unorm` — the no-alpha-format dst-alpha=1 behavior). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pipeline/overrides.spec.ts` | partial (sync) | 1 / 4 (`basic`, isAsync=false) | `precision`, `shared_shader_module`, `multi_entry_points`, isAsync=true (deferred V20b/c) | **T50 (V20) — pipeline-overridable constants.** WGSL `override` constants in the vertex (`xright`/`ytop`) + fragment (`R`/`G`/`B`/`A`) stages set via `WGPUConstantEntry` on the vertex/fragment state. `basic`, `isAsync=false`, 6 subcases (defaults, vertex override collapsing the triangle → clear color, fragment color overrides); `1×1 bgra8unorm` (B,G,R,A byte order) readback. **Dawn (6) and wgpu-native pass.** Finding: **F-047** — yawgpu **ignores override constants** (read as 0 — neither the WGSL default nor the pipeline value applied; fragment outputs `{0,0,0,0}`), `pass=1 fail=5`, **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `fff8634` (apply overridable constants in shader codegen; render **and** compute) — re-test render `pass=6` + compute `pass=1`, `fail=0` on both HALs, surfaced/unmasked. Deferred **V20b** (isAsync) / **V20c** (`precision`, `shared_shader_module`, `multi_entry_points`). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pipeline/vertex_only_render_pipeline.spec.ts` | N/A (unimplemented upstream) | 0 / 1 (`.unimplemented()`) | — | Upstream registers the single test `draw_depth_and_stencil_with_vertex_only_pipeline` as `.unimplemented()` (a TODO stub with no body); this port mirrors it (registered `.unimplemented`) so the suite catalog stays faithful. No GPU coverage until upstream implements it. |
| `api/operation/render_pipeline/sample_mask.spec.ts` | partial (minimal) | 1 / 2 (`fragment_output_mask`, color+depth+stencil) | sampleCount=1 + interp + full mask matrix + `alpha_to_coverage_mask` (deferred V21b-rest) | **T59 (V21) — MSAA sample mask; opens `sample_mask`.** Final per-sample coverage = AND(rasterization mask, pipeline `multisample.mask`, fragment `@builtin(sample_mask)` output). Minimal slice: `sampleCount=4`, flat fragment, **color attachment only**; 6 literal (raster, sampleMask, fragMask) combos isolating each mask; per-sample readback via a `texture_multisampled_2d<f32>` compute pass (mirrors upstream `copy2DTextureToBufferUsingComputePass`) — **no new harness**. **Dawn and yawgpu-Vulkan/MoltenVK pass all 6.** Findings: **F-051** — yawgpu's **Metal HAL crashed** creating a default `createView()` of the multisampled target (built `MTLTextureType2D` instead of `2DMultisample`), Metal-HAL-only — now **RESOLVED** (yawgpu `c29dc78`-era; `pass=6 fail=0 crash=0` on both HALs, removed from the crash-list); **F-052** — **wgpu-native ignores the pipeline `multisample.mask`** (the 3 cases with `mask≠0xF` fail; `pass=3 fail=3`), **OPEN** (wgpu-native). **T73 (V21b)** extends the 6 cases to ALSO verify the **depth** and **stencil** aspects per sample (adds a `sampleCount=4 depth24plus-stencil8` attachment, `depthWriteEnabled`/`depthCompare always` + stencil `replace`/`ref 0xff`, and two more `texture_multisampled_2d<f32>`/`<u32>` aspect compute readbacks): covered samples get depth `0.0`/stencil `0xff`, others the clear `1.0`/`0`. **Dawn and yawgpu (Metal + MoltenVK) pass all 6**; **wgpu-native still fails the same 3** (F-052, `mask≠0xF`). The MSAA depth/stencil aspect readback exercises the (now-fixed) F-051 view path on yawgpu — **no new finding**. Deferred **V21b-rest**: sampleCount=1, interp entry, full mask matrix, `alpha_to_coverage_mask`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/vertex_state/index_format.spec.ts` | partial (uint16/uint32) | 2 / 6 (`index_format,uint16`, `index_format,uint32`) | `index_format,{change_pipeline_after_setIndexBuffer, setIndexBuffer_before_setPipeline, setIndexBuffer_different_formats}` + `primitive_restart` (deferred) | **T41 (V11) — the indexed-draw index-format path; opens the `vertex_state` area.** First `setIndexBuffer`(`uint16`/`uint32`, byte offset) + `drawIndexed`: a fixed quad is drawn indexed from `[1,2,0,0,0,0,0,1,3,0]` into an `8×4 r8uint` target (a `u32` fragment), read back and per-texel compared to the expected raster (full square / bottom-left triangle / nothing). The index format + offset must be interpreted correctly (a `uint16` buffer read as `uint32`, or a wrong offset, renders nothing). Reuses the T30 `drawIndexed` + `makeBufferWithContents(INDEX)` + a local `r8uint` per-texel readback — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass 6** — **no findings** (yawgpu interprets the index format + offset correctly). Deferred: the index state-tracking tests (`change_pipeline_after_setIndexBuffer`, `setIndexBuffer_before_setPipeline`, `setIndexBuffer_different_formats`) + `primitive_restart`; `vertex_state/correctness.spec.ts` (the vertex-attribute format matrix). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/3d_texture_slices.spec.ts` | partial (rgba8unorm) | 2 / 3 (`one_color_attachment,mip_levels`, `multiple_color_attachments,same_mip_level`) | `multiple_color_attachments,same_slice_with_diff_mip_levels` + format/sameTexture/samePass breadth (deferred V13b/c-rest/d) | **T43 (V13) — the render-to-3D-texture-slice path.** First `WGPURenderPassColorAttachment.depthSlice`: a half-covering triangle is rendered into **slice `depthSlice` of mip `mipLevel`** of a `dimension:'3d'` `rgba8unorm` texture sized `[4<<mip, 4<<mip, 2<<mip]` (so the target mip is always `4×4×2`), then that mip is read back (raw `copyTextureToBuffer` with `mipLevel` + `rowsPerImage` + a 3D extent) and per-texel/per-slice compared: the rendered slice = shader output `{11,21,31,41}` where `x≤y` / clear `{77,102,128,153}` where `x>y`, every other slice = 0. Ports `one_color_attachment,mip_levels` (`mipLevel∈{0,1,2}` × `depthSlice∈{0,1}` = 6 cases). Reuses the V1/V3 color render-pass + a local 3D readback (the heavy upstream `TexelView`/`readTextureToTexelViews` machinery is replaced by a direct byte check) — **no new harness**. **Dawn (oracle) and wgpu-native both pass all 6.** Finding: **F-043** — yawgpu **ignored `depthSlice`** and always rendered to **slice 0** (the 3 `depthSlice=1` cases failed `at (0,0,0): expected 0, got 11` — slice 0 held the pattern; `depthSlice=0` passed at all mip levels so mip routing was fine), **cross-HAL** (Metal == Vulkan/MoltenVK byte-identical, `pass=3 fail=3`), now **RESOLVED** in yawgpu `c6935f7` (the attachment's `depthSlice` is now threaded into the 3D render-target view) — re-test `pass=6 fail=0` on **both** HALs (Dawn/wgpu-equal), surfaced/unmasked. **T62 (V13c)** adds `multiple_color_attachments,same_mip_level` (minimal: rgba8unorm, mip 0, `sameTexture=true`, `samePass=true`, fixed `attachmentCount=4`) — 4 color attachments each a different `depthSlice` (0..3) of one `4×4×16` 3D texture, a 4-output fragment shader, one pass; verify slice `i` covered → location-`i` bytes, slices ≥4 → zero. Finding: **F-053** — yawgpu **rendered nothing** to the multi-attachment 3D slices (slice 0 read `0`; `pass=0 fail=1`), cross-HAL; Dawn + wgpu-native pass. Now **RESOLVED** — yawgpu `c29dc78`-era; `pass=1` on Metal **and** green on native Windows/NVIDIA Vulkan (user-confirmed). The residual MoltenVK failure (an explicit `[mvk-error]` `vkCreateImageView` 2D-view-on-3D-image limitation) is a **confirmed MoltenVK-only artifact** (like F-033/F-045), not a yawgpu defect. (The single-attachment F-043 is fixed; this multi-attachment path is distinct.) Deferred **V13b**: the `kPossibleColorRenderableTextureFormats` matrix + sint/uint `d=1`; **V13c-rest/d**: `sameTexture=false`, `samePass=false`, `mipLevel=1`, `same_slice_with_diff_mip_levels`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/rendering/indirect_draw.spec.ts` | ported | 1 / 1 (`basics`) | — | **T42 (V12) — the tightly-packed indirect-draw-parameter path.** Verifies `drawIndirect`/`drawIndexedIndirect` read their args struct *tightly packed* from an `INDIRECT` buffer at a byte offset: the **expected** call (at `indirectOffset`) draws the bottom-left triangle while the surrounding slots hold *other* draw args (right-top triangle / both / nothing) + deterministic `100u` filler, so a wrong-offset read fills the wrong pixels. Renders a green fragment into a `4×4 rgba8unorm` target and checks `(0,1)`=filled / `(1,0)`=not-filled. Ports `basics` over `isIndexed{true,false}` × 8 byte offsets (`{0, 4, k·size, k·size+4}` for `k∈{1,3,99}`) = 16 subcases. Reuses the T30/V3 vertex-buffer-layout (`Float32x2`) + `setVertexBuffer`/`setIndexBuffer` + `drawIndirect`/`drawIndexedIndirect` + a local 2-pixel `rgba8unorm` check — **no new harness**. Intentional deviation: upstream's `Math.random()` filler → deterministic `100u` (reproducible; a wrong-offset read still lands on non-bottom-left args and fails). **Dawn (oracle) pass 16, yawgpu (Metal) pass 16, wgpu-native (`--isolate`) pass 2** — **no findings** (yawgpu reads the indirect-param offset/packing correctly). Deferred: `indirect_draw_check_buffer_state.spec.ts` (separate validation file). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/resource_init/texture_zero.spec.ts` | partial (minimal) | 1 / 1 (`uninitialized_texture_is_zero`, minimal subset) | the generic `check_texture` matrix (deferred V29b) | **T57 (V29) — texture zero-init; opens `resource_init`.** An uninitialized texture reads back **zero** (WebGPU lazily zero-inits subresources on first read). Minimal hand-written slice: create a `4×4 2D` texture (`rgba8unorm`/`r32uint`, `RENDER_ATTACHMENT \| COPY_SRC`), **never write it**, `copyTextureToBuffer`, verify all texels 0. 2 cases (no generic `check_texture`/`TextureZeroInitTest` machinery). **Dawn, yawgpu, wgpu-native all pass 2** — **no findings** (yawgpu zero-inits textures). Deferred **V29b**: the full param matrix (formats/dimensions/sampleCount/mip/array + `Sample`/`CopyToTexture`/`DepthTest`/`StencilTest` read methods + `StoreOpClear` + canary); **V29c**: `resource_init/buffer.spec.ts` (16 buffer zero-init tests). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/resource_init/buffer.spec.ts` | partial (minimal) | 6 / 14 (first-use zero-init: partial_write, copy-src, uniform, storage, vertex, index) | map/`mapped_at_creation` paths, indirect, query-resolve, copy-to-texture, readonly-storage, the offset/usage/mapMode matrices (deferred V29d) | **T61 (V29c) — buffer lazy zero-init.** An uninitialized `GPUBuffer` reads back **zero** on first use across structurally-distinct read paths. Minimal slice (each at offset 0): `partial_write_buffer`, `copy_buffer_to_buffer_copy_source`, `uniform_buffer`, `storage_buffer`, `vertex_buffer`, `index_buffer` = 6 cases; the bind/draw paths use a green-if-zero shader → 1×1 output-texture readback (`copyTextureToBuffer` + `expectGPUBufferValuesPassCheck`) — **no new harness**. **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass 6** — **no findings** (all backends zero-init buffers). Deferred **V29d**: the mapping paths (`map_whole_buffer`/`map_partial_buffer`/`mapped_at_creation_*`), `indirect_buffer_for_{draw,dispatch}_indirect`, `resolve_query_set_to_partial_buffer`, `copy_buffer_to_texture`/`copy_texture_to_partial_buffer`, `readonly_storage_buffer`, the bufferOffset/usage/mapMode subcase matrices. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/texture_view/format_reinterpretation.spec.ts` | todo (deferred V31) | 0 / 2 | `texture_binding`, `render_and_resolve_attachment` (deferred V31) | Deferred: viewing a texture as a different-but-compatible (srgb ↔ non-srgb) format — needs the `TexelView` decode-comparison stack + sRGB transfer math. A focused phase (V31). |
| `api/operation/texture_view/write.spec.ts` | partial (minimal) | 1 / 3 (`format`, rgba8unorm × 3 non-MSAA methods) | `dimension`, `aspect` (`.unimplemented()` upstream) + the `format` MSAA/full-matrix/`viewUsageMethod` breadth (deferred V31b) | **T60 (V31a) — write through a texture view; opens `texture_view`.** Writing into a `16×16 rgba8unorm` texture **through its `createView()`** then reading every texel back. Minimal slice: `format=rgba8unorm`, `sampleCount=1`, `viewUsageMethod=inherit`, the 3 non-MSAA write methods (`storage-write-compute`, `storage-write-fragment`, `render-pass-store`) = 3 cases; readback via `copyTextureToBuffer` + `expectGPUBufferValuesPassCheck` (±1 byte). **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass 3** — **no findings**. Deferred **V31b**: `render-pass-resolve`, `sampleCount=4`, the full `kRegularTextureFormats` matrix (float/sint/uint), `viewUsageMethod=minimal`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/texture_view/texture_component_swizzle.spec.ts` | todo (deferred V31) | 0 / 1 | `read_swizzle` (deferred V31) | Deferred: the `texture-component-swizzle` feature (component remapping on a view) — feature-gated + niche. A focused phase (V31). |
| `api/operation/sampling/sampler_texture.spec.ts` | todo (deferred V28) | 0 / 1 | `sample_texture_combos` (deferred V28) | Deferred: `sample_texture_combos` is a **max-resources stress test** (the maximum number of sampled textures × samplers × storage textures per stage, with a generated shader) — better as a focused phase than a quick slice. The `sampling` structural foundation is covered by `filter_mode` (V6). |
| `api/operation/sampling/anisotropy.spec.ts` | todo (deferred V28) | 0 / 2 | `anisotropic_filter_{checkerboard, mipmap_color}` (deferred V28) | Deferred: anisotropic filtering (a `maxAnisotropy` sampler + oblique checkerboard/mip geometry) — a niche, geometry-heavy path; deferred to a focused phase. |
| `api/operation/sampling/filter_mode.spec.ts` | partial (nearest-filter foundation) | 2 / 5 (`magFilter,nearest`, `minFilter,nearest`) | `magFilter,linear`, `minFilter,linear`, `mipmapFilter` (registered `unimplemented`) | **T34 (V6) — the texture-sampling foundation; opens the `sampling` area.** First `GPUSampler` + `textureSample` path: a sampler (`addressModeU`/`V` + `magFilter`/`minFilter` `nearest`) + a `sampler`+`texture_2d<f32>` bind group + a `textureSample(t,s,uv)` fragment, sampling a 2×2 `rgba8unorm` checker into a **6×6 instanced-quad grid** (`draw(6,36)`) whose exact pattern reveals the address-mode wrapping. Ports the `nearest` tests over the 3×3 `addressModeU × addressModeV` matrix (the 9 `kNearest*` 6×6 tables, verbatim; `nearest` ⇒ exact texel selection ⇒ exact byte compare, `maxDiff=0`) on **`rgba8unorm`**. Reuses the T30 instancing + the T31 rgba8unorm byte check + `createSamplerTracked` — **no new harness**. **Dawn (oracle), yawgpu, and wgpu-native all pass 18 (`pass=18 skip=3 fail=0`)** — **no findings** (the first new structural path that yawgpu handled cleanly). Deferred to **V6b**: `magFilter,linear`/`minFilter,linear` (the 4×4 `kLinear*` tables + interpolation tolerance); **V6c**: `mipmapFilter` (mip pyramid + LOD); plus a format-breadth follow-up (all filterable color formats + the `rgba32float` render-target fallback). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/memory_sync/buffer/multiple_buffers.spec.ts` | todo (deferred V32) | 0 / 6 | `rw`, `wr`, `ww`, `multiple_pairs_of_*` (deferred V32) | Deferred: the `single_buffer` sync model across **multiple** buffers (the same `rw`/`wr`/`ww` boundary machinery + multiple-pairs-of-draws/dispatches) — breadth on the V7 foundation; a focused phase (V32). |
| `api/operation/memory_sync/texture/same_subresource.spec.ts` | partial (minimal) | 3 / 5 (`rw`, `wr`, `ww`, copy-based slice) | the `TextureSyncTestHelper` op×context×boundary matrix (render/sample/storage/resolve ops, finer boundaries); `rw,single_pass,*` are `.unimplemented()` upstream (deferred V32b-rest) | **T76 (V32b) — texture same-subresource memory sync.** Read/write to the **same texture subresource** across a **command-buffer boundary** (two command buffers, one submit) must be ordered. Minimal copy-based slice (mirrors the V7 `single_buffer` model for a texture): read = `t2b-copy`, write = `b2t-copy`. `rw` — read-before-write reads the pre-write value; `wr` — write-before-read reads the written value; `ww` — second write wins. `1×1 rgba8unorm`; the array order in `wgpuQueueSubmit(queue, 2, …)` is the exec order. Reuses copy + buffer readback — **no new harness**. **Dawn, yawgpu (Metal + MoltenVK), wgpu-native all pass 3** — **no findings** (copy-based texture-subresource ordering is correct everywhere). Deferred **V32b-rest**: the render/sample/storage/resolve ops + finer boundaries + the full `TextureSyncTestHelper`. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/memory_sync/texture/readonly_depth_stencil.spec.ts` | partial | 1 / 1 (`sampling_while_testing`, all 4 read-only/written combos for depth24plus-stencil8) | the `kDepthStencilFormats` matrix (depth-only / stencil-only formats) (deferred V32a-rest2) | **T72 (V32a) — read-only depth-stencil + concurrent sampling; opens `memory_sync/texture`.** In one render pass a `depth24plus-stencil8` texture is bound as a **read-only** depth+stencil attachment (depth/stencil tested) **and** its depth-only/stencil-only aspects are sampled in the fragment shader; an on-GPU check pass re-samples and writes `1` per texel on match (verified via a `3×3 r32uint` readback). Minimal combo: `depthReadOnly=true, stencilReadOnly=true`. Reuses depth/stencil aspect views + `copyTextureToBuffer` — **no new harness**. **Dawn + wgpu-native pass.** Finding: **F-055** — yawgpu read back **wrong** values (`texel (0,0) expected 1, got 0`), **cross-HAL** (Metal == MoltenVK), now **RESOLVED** (yawgpu `79c4968`-era; re-test `pass=1 fail=0` on both HALs + native Windows Vulkan). **T74 (V32a-rest)** parameterizes the test over all 4 `(depthReadOnly, stencilReadOnly)` combos (adds the **write-while-sample** combos: one aspect read-only+sampled, the other written via `depthWriteEnabled`/`increment-clamp`, with `fakeDepth`/`fakeStencil` filling the written aspect's bind slot, the test/check WGSL generated per combo). **Dawn + yawgpu (Metal + MoltenVK) pass all 4.** Finding: **F-056** — **wgpu-native aborts** on the 2 **mixed** combos (`signal 6`: `conflicting usages … DEPTH_STENCIL_WRITE … exclusive`) — its usage-scope validation treats the written aspect's `DEPTH_STENCIL_WRITE` as conflicting with the `RESOURCE` sample of the read-only aspect (not tracked per-aspect) and aborts; **OPEN** (wgpu-native; the both-read-only / both-written combos pass). Deferred **V32a-rest2**: depth-only / stencil-only formats. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/memory_sync/buffer/single_buffer.spec.ts` | ported | 6 / 6 (`rw`, `wr`, `ww`, `two_dispatches_in_the_same_compute_pass`, `two_draws_in_the_same_render_pass`, `two_draws_in_the_same_render_bundle`) | — | **T35 (V7) — the buffer memory-synchronization foundation; opens the `memory_sync` area.** Write a buffer via op A, cross a **boundary**, read/write via op B, verify the boundary enforced ordering (`rw` ⇒ the read sees the *old* value `0`; `wr` ⇒ the *new* value `1`; `ww` ⇒ the *second* write `2`). Ports a **tractable subset** — writeOps `{storage (compute pass), b2b-copy}`, readOps `{storage-read (compute pass), b2b-copy}`, boundaries `{command-buffer (two command buffers, one submit), queue-op (two submits)}` — plus the self-contained `two_dispatches_in_the_same_compute_pass`. Reuses the V2 compute-storage + T22 `copyBufferToBuffer`/`expectGPUBufferValuesEqual` — **no new harness**. **Dawn (oracle) and wgpu-native both pass all 25 (`pass=25 fail=0`).** Finding: **F-039** — yawgpu's `two_dispatches_in_the_same_compute_pass` read back `0` instead of `2` (both writes lost) **only under batch / `--isolate`** while passing in isolation (the `rw`/`wr`/`ww` cross-boundary cases all passed); **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `89f25df` (compute dispatch is now a per-dispatch usage scope, not whole-pass) — re-test `pass=25 fail=0` across `--workers`/no-workers/`--isolate` on both HALs, regression sweep `pass=926 fail=0`. Surfaced/unmasked. **T39 (V7b)** then ports the two `two_draws_*` write-after-write tests, opening the **render-stage storage write** (a fragment shader writing `var<storage, read_write>` from a `point-list` draw) + **render bundles** (`GPURenderBundleEncoder` → `finish` → `executeBundles`); the in-pass write-after-write is loose so the buffer must hold `1` **or** `2`. **Dawn (oracle) and wgpu-native pass all 5** `two_draws_*` cases. Finding: **F-042** — yawgpu's **render-stage (fragment) storage write read back `0`** (all 5 failed, incl. the non-bundle subcase ⇒ not bundle-specific; the compute storage write worked), **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `042902b`+`eadc2f6` (render usage scope now allows write+write across draws + executes render-bundle draws) — re-test `pass=5 fail=0` on both HALs, broad regression sweep `pass=946 fail=0`. Surfaced/unmasked. Still deferred: the full `rw`/`wr`/`ww` render/draw/index/indirect/uniform op axis + remaining boundaries + `multiple_buffers`; **V7c**: `texture/{same_subresource, readonly_depth_stencil}` sync. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pass/clear_value.spec.ts` | partial (stencil) | 1 / 2 (`stencil_clear_value`) | `loaded` (unimplemented upstream) | **T51 (V22) — the stencil clear-value path.** Clear a `1×1` stencil attachment to `stencilClearValue` (masked to the low 8 bits), draw a quad with `stencilCompare:equal` + `setStencilReference`, verify the color is green (stencil test passed) + the stencil aspect reads back `stencilClearValue & 0xff`. 3 stencil formats × 5 clear values × 2 (apply-as-reference) = 30 cases. Reuses the T33 stencil render + T27 stencil-aspect readback — **no new harness**. **Dawn (oracle) passes all 30.** Finding: **F-048** — yawgpu **and wgpu-native** fail the 6 cases where the reference is the **unmasked** out-of-range clear value (258/65539): the **stencil reference is not masked to the stencil aspect's bit width** before the compare (the clear-value masking works), `pass=24 fail=6`, yawgpu **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED for yawgpu** in `9bc49dc` (mask the stencil reference to the 8-bit aspect width) — re-test `pass=30 fail=0` on both HALs; **wgpu-native still affected**. Surfaced/unmasked. `loaded` is `.unimplemented()` upstream. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pass/storeOp.spec.ts` | partial (4 tests, core params) | 4 / 4 (all four `render_pass_store_op,*` tests) | the format/mip/array-layer + `colorAttachments {1,3,4}` + combined depth-stencil-format breadth (deferred V23c) | **T52 (V23) — render-pass storeOp.** An **empty** render pass clears then `store`s/`discard`s; `store` keeps the clear value, `discard` → 0 (lazily cleared). `color_attachment_only` (rgba8unorm, 2 cases) + `color_attachment_with_depth_stencil_attachment` (color × depth, 4 cases). Color `2×2` + `depth32float` aspect readback. **T64 (V23b)** adds `multiple_color_attachments` (fixed `colorAttachments=2`, storeOp1×storeOp2 = 4 cases; per-attachment store fan-out) + `depth_stencil_attachment_only` (`{depth32float, stencil8}` × storeOp = 4 cases; color-attachment-less depth-only / stencil-only pass, adds a stencil-aspect readback). Reuses the V1 render pass + V23 helpers — **no new harness**. **Dawn, yawgpu (Metal), wgpu-native all pass** (6 + 8) — **no findings**. Deferred **V23c**: `colorAttachments {1,3,4}`, combined depth-stencil formats + both-aspect check, mip/array-layer + color-format matrix. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pass/transient_attachment.spec.ts` | ported | 2 / 2 (`increasing_attachments_count`, `overlapping_transient_attachments`) | — | **T53 (V24) — transient (memoryless) attachments.** Create `TRANSIENT_ATTACHMENT` color targets in empty render passes (clear + `discard`); `increasing_attachments_count` (1..`maxColorAttachments`) + `overlapping_transient_attachments` (circular `(T1,T2),(T2,T3),(T3,T1)` to stress the transient-memory allocator). Gated on `skipIfTransientAttachmentNotSupported()`; no readback (passes on clean submit). **Dawn, yawgpu, wgpu-native all skip** (none expose `TRANSIENT_ATTACHMENT` on these HALs) — the test + skip gate are faithful; **no findings** (no coverage until a backend supports the usage). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pass/resolve.spec.ts` | partial (MSAA-resolve core) | 1 / 1 (`render_pass_resolve`, param subset) | — (breadth deferred to V8b) | **T36 (V8) — the multisample-resolve foundation; opens the `render_pass` area.** First **MSAA** path: a `4×4 sampleCount=4` color attachment + a single-sample `resolveTarget` (`multisample.count=4`); a white triangle covers the top-left half, the resolve averages the multisampled edge → the resolve target reads **`(0,0)`=white**, **`(3,3)`=black**, **`(3,0)`=`0.5`** (midpoint, `maxDiff≈2/255`). Ports the `render_pass_resolve` test on `rgba8unorm` over `separateResolvePass{false,true}` × `storeOperation{store,discard}` × `slotsToResolve{[0,2],[1,3],[0,1,2,3]}` (12 subcases; `numColorAttachments=2`, mip0/layer0; the separate empty load-resolve pass is the known-driver-bug case). Reuses the T28/T31 color-render + a local 4×4 rgba8unorm pixel check — **no new harness**. **Dawn (oracle) and wgpu-native both pass all 12 (`pass=12 fail=0`).** Finding: **F-040** — yawgpu's **multisample resolve did not write the resolve target** (all 12 subcases failed `(0,0) expected 1 got 0`, `storeop2` passed); **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `bc8c280`+`3303058` (added MSAA render pipelines + multisample resolve + multi-color-attachment support) — re-test `pass=12 fail=0` on both HALs, regression sweep `pass=951 fail=0`. Surfaced/unmasked. Deferred to **V8b**: the color-format matrix, `numColorAttachments=4`, mip/array-layer-offset resolve, transient + depth-stencil attachments. Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/render_pass/storeop2.spec.ts` | ported | 1 / 1 (`storeOp_controls_whether_1x1_drawn_quad_is_stored`) | — | **T36 (V8).** A `1×1 r8unorm` render target; draw a red triangle; `storeOp:'store'` ⇒ stored (`R=255`), `storeOp:'discard'` ⇒ cleared (`R=0`). Verifies the color store-op semantics (local `r8unorm` byte readback). **Dawn, yawgpu, and wgpu-native all pass 2** — no findings (single-sample render + store works on yawgpu; the MSAA gap is F-040). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/storage_texture/read_only.spec.ts` | partial (read-only core) | 1 / 1 (`basic`, param subset) | — (breadth deferred to V9b) | **T37 (V9) — the read-only storage-texture foundation; opens the `storage_texture` area.** First `texture_storage_2d<format, read>` + `textureLoad` path: a `16×8` storage texture filled with a known per-texel value, read in a compute shader (`@workgroup_size(16,8,1)`, one invocation per texel) into an output storage buffer, verified `== [value,0,0,1]` per texel. Ports `read_only:basic` for `shaderStage=compute`, `dimension=2d`, 1 layer, formats `{r32uint, r32sint, r32float}` (one per type class, single-channel — the 3 data-gen formulas verbatim). Reuses the V2 compute-storage + T24b `queueWriteTexture` + T22 `expectGPUBufferValuesEqual` — **no new harness**. **Dawn (oracle) and wgpu-native both pass all 3.** Finding: **F-041** — yawgpu's **read-only storage-texture `textureLoad` read back zero** (output buffer all zeros; all 3 cases failed), while the compute storage-*buffer* path (V2) worked; **cross-HAL** (Metal == Vulkan/MoltenVK), now **RESOLVED** in yawgpu `2e4edb7` (wired storage-texture bindings + MSL runtime-array buffer sizes) — re-test `pass=3 fail=0` on both HALs, broad regression sweep `pass=965 fail=0`. Surfaced/unmasked. Deferred to **V9b**: the format matrix (multi-channel/packed/unorm decode), vertex/fragment stages, 1D/3D, array layers; **V9c**: `read_write.spec.ts` (`textureLoad` + `textureStore`). Fixture `AllFeaturesMaxLimitsGpuTest` |
| `api/operation/storage_texture/read_write.spec.ts` | partial (read-write core) | 1 / 1 (`basic`, param subset) | — (breadth deferred to V9d) | **T38 (V9c) — the read-write storage-texture path.** First `texture_storage_2d<format, read_write>` + `textureStore` + `textureBarrier()` path: a `16×8` storage texture filled with `baseValue=2*i+1` data is reversed in both axes by a compute shader (each invocation `textureLoad`s the mirror texel `(W-1-x, H-1-y)`, `textureBarrier()`s, then `textureStore`s at `(x,y)`), then read back (`copyTextureToBuffer`) and per-texel compared against the mirrored input. Ports `read_write:basic` for `shaderStage=compute`, `2d`, 1 layer, formats `{r32uint, r32sint, r32float}` (the canonical read-write storage formats). Reuses the T37/V9 storage-texture pattern (with `ReadWrite` access) + the T36/V8 per-texel readback — **no new harness**. **Dawn, yawgpu, and wgpu-native all pass 3** — **no findings** (yawgpu handles the storage-texture write + `textureBarrier` correctly; the read path's F-041 fix covered it). Deferred to **V9d**: the `fragment` stage, 1D/3D, array layers, the read-write format matrix. Fixture `AllFeaturesMaxLimitsGpuTest` |

## Maintenance

- After porting a file: set its row here, update the **Area summary** counts, and confirm
  `cts --list 'webgpu:<file>:*'` matches the upstream case count.
- When re-baselining (see [UPSTREAM.md](UPSTREAM.md)): diff the new upstream listing against ours
  to find added/removed/renamed files and changed case counts; reflect them here.
- A planned `tools/coverage` step automates the listing diff and can emit a fresh area summary —
  see [05-porting-guide §7](05-porting-guide.md).
