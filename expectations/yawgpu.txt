# yawgpu expectations — captured against commit e56f30a5db13d03f4ec4268b426e0c090de579b4 (2026-06-02).
# One full case query per line (# comments allowed; a line ending ':*' is a test-prefix match).
#
# yawgpu passes every ported test — both api,validation and api,operation — on real-GPU Metal
# (api,validation: pass=4332 skip=383 fail=0 crash=0; api,operation,command_buffer: pass=5 fail=0 crash=0).
# It passes every test it runs and additionally runs the 8 immediate_data_size cases that Dawn skips
# (yawgpu reports maxImmediateSize=64; Dawn/wgpu-native report 0). Every finding this suite surfaced
# for yawgpu has been fixed upstream:
#   F-005, F-006, F-008, F-009, F-010, F-011, F-014, F-016, F-018, F-020, F-022, F-023.
# F-015 (createView view-usage subset) is wgpu-native-only — yawgpu enforces it.
# This file therefore has NO expected failures. See docs/FINDINGS.md.
