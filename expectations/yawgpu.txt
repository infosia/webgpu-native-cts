# yawgpu expectations — captured against commit 925520adf6ec7238679f5eb074090cfb37ecbd0c (2026-06-01).
# One full case query per line (# comments allowed; a line ending ':*' is a test-prefix match).
#
# One OPEN yawgpu finding: F-020 (the line below). Earlier findings
# F-005/006/008/009/010/011/014/016/018 were all fixed upstream; F-015 (createView view-usage subset) is
# wgpu-native-only — yawgpu enforces it.
#   F-020 (createPipelineLayout T18): yawgpu does not implement null bind-group-layout slots — it rejects
#   a NULL BGL element ("bindGroupLayouts elements must not be null") where Dawn accepts it.
# See docs/FINDINGS.md. Add a query line here only if a future test surfaces a new yawgpu defect.

# F-020: null bind-group-layout slots in createPipelineLayout (yawgpu rejects; Dawn accepts).
webgpu:api,validation,createPipelineLayout:bind_group_layouts,null_bind_group_layouts:*
