# yawgpu expectations — captured against commit f75fc0ac04e8395afbe81778bab0a451040170f2 (2026-06-01).
# One full case query per line (# comments allowed; a line ending ':*' is a test-prefix match).
#
# One OPEN yawgpu finding: F-022 (the 2 lines below). Earlier findings
# F-005/006/008/009/010/011/014/016/018/020 were all fixed upstream; F-015 (createView view-usage subset)
# is wgpu-native-only — yawgpu enforces it. yawgpu also runs the 8 immediate_data_size cases Dawn skips
# (yawgpu reports maxImmediateSize=64; Dawn/wgpu-native report 0).
#   F-022 (createPipelineLayout T21): yawgpu rejects pipeline creation when a shader-visible uniform
#   buffer binding has the default minBindingSize=0 ("buffer minBindingSize is too small"); Dawn defers
#   the size check to bind-group usage and accepts it.
# See docs/FINDINGS.md. Add a query line here only if a future test surfaces a new yawgpu defect.

# F-022: minBindingSize=0 not deferred at pipeline creation (yawgpu rejects; Dawn accepts).
webgpu:api,validation,createPipelineLayout:bind_group_layouts,create_pipeline_with_null_bind_group_layouts:*
webgpu:api,validation,createPipelineLayout:bind_group_layouts,set_pipeline_with_null_bind_group_layouts:*
