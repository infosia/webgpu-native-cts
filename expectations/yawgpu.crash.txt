# yawgpu known-crash list — for --crash-list selective isolation.
# Same line format as --expectations: exact case query, or a line ending ':*' for a prefix.
# Cases listed here are forked into a child process; all others run in-process.
#
# Regenerate after a yawgpu revision bump with:
#   cts.exe <queries> --isolate --emit-crash-list expectations/yawgpu.crash.txt
#
# F-051 (Metal HAL only): creating a default view of a multisampled texture aborts with
#   "MTLTextureType2DMultisample not compatible with texture view textureType (MTLTextureType2D)".
#   yawgpu's Vulkan HAL (MoltenVK / native Windows) passes all 6 cases. See docs/FINDINGS.md.
webgpu:api,operation,render_pipeline,sample_mask:*
